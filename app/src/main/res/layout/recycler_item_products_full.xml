<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="viewModel"
            type="com.vag.lmsapp.app.products.ProductItemFull" />
    </data>
    <com.google.android.material.card.MaterialCardView
        style="@style/RoundCard.NoElevation"
        android:layout_marginHorizontal="@dimen/activity_horizontal_margin"
        android:layout_marginVertical="4dp"
        app:contentPadding="0dp"
        android:id="@+id/cardDiscount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout
            android:layout_margin="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:divider="@drawable/divider_10"
            android:showDividers="middle"
            android:orientation="horizontal">
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:divider="@drawable/divider_10"
                android:showDividers="middle"
                android:orientation="vertical">
                <TextView
                    style="@style/TileSubTitle"
                    android:id="@+id/textTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:text="Ariel"
                    android:text="@{viewModel.product.name}"/>
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:text="DETERGENT"
                    android:text="@{viewModel.product.productType.toString()}"/>
            </LinearLayout>
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:divider="@drawable/divider_10"
                android:showDividers="middle"
                android:gravity="end">
                <TextView
                    style="@style/TextPrimary"
                    android:peso="@{viewModel.product.price}"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="P 15.00"/>
                <TextView
                    style="@style/Bold"
                    android:text="@{viewModel.product.currentStockStr()}"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="P 15.00"/>
            </LinearLayout>
        </LinearLayout>
        <com.google.android.material.card.MaterialCardView
            android:visibility="@{viewModel.product.hidden}"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:cardBackgroundColor="@color/card_disabled_cover"
            app:cardElevation="0dp">

        </com.google.android.material.card.MaterialCardView>
    </com.google.android.material.card.MaterialCardView>
</layout>